>>  z-tokens exchange keys --help


  -s
  --sender  {enabled}
    **  generate sender key pair

  -r
  --recipient  {enabled}
    **  generate recipient key pair

  -x
  --secret  {enabled}
    **  generate shared secret

  -e
  --seed  {enabled}
    **  generate shared seed

  -b
  --ballast  {enabled}
    **  generate shared ballast

  -p
  --pin  {enabled}
    **  generate shared PIN

  -o
  --self  {enabled}
    **  generate one key, and encode it both for sending and receiving
    !!  CAUTION

  -c
  --comments  {enabled}
    **  output comments

  -v --version

  -h --help

